%h1= t("liquid_template.new")
= form_for(:liquid_template, :url => admin_liquid_theme_liquid_templates_path(params[:liquid_theme_id])) do |f|
	= render :partial => "form", :locals => { :f => f }
	= render :partial => "admin/shared/new_resource_links"